# MD Preview

Live Markdown previewer with GFM support and code syntax highlighting. Write markdown on the left, see rendered output on the right.

## Features

- Split editor/preview with live rendering
- GitHub Flavored Markdown (tables, task lists, strikethrough)
- Code syntax highlighting via rehype-highlight
- Toolbar shortcuts (bold, italic, heading, list, code, link, image, table)
- Word and character count
- Sample document templates

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
